export interface StudentType {
  tdt_id: string;
  name: string;
  role: string;
  gender: string;
  phoneNumber: string;
  parentPhoneNumber: string;
  address: string;
  dateOfBirth: Date;
  email: string;
  advisorId: string;
  class: string;
}
